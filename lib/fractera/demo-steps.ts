import type { ParseStepData } from "@/lib/types";

// 🔒 ХАРДКОР-ВИТРИНА ТИПОВ ШАГА — РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-09-03, ДОСЛОВНО:
// «независимо от того какой получит вопрос в чате будет возвращать всегда
// этот элемент: chain of thinking чтобы там ты показал все существующие типы».
// Живёт ПОСТОЯННО, для ЛЮБОГО вопроса, пока владелец не скажет убрать — это
// его выбор через явный вопрос, а не временный флаг.
//
// 🛑 ЭТО ДЕМОНСТРАЦИЯ, А НЕ НАСТОЯЩИЙ РАЗБОР ЗАПРОСА. Шесть строк ниже не
// читают базу, не ходят в реестр и не вызывают инструмент — они лишь
// показывают, КАК будет выглядеть настоящий разбор, когда его перенесут сюда
// (шаг 101 прямо запретил проектировать эту логику вперёд; это её витрина,
// не реализация).
export const DEMO_PARSE_STEPS: readonly Omit<ParseStepData, "status">[] = [
  { id: "demo-text", label: "Сохранение сообщения" },
  { id: "demo-db", label: "Чтение базы данных" },
  { id: "demo-registry", label: "Поиск в реестре признаков" },
  { id: "demo-tool", label: "Вызов инструмента" },
  { id: "demo-image", label: "Проверка изображения" },
  { id: "demo-link", label: "Проверка ссылки" },
] as const;

/** Сколько показываются точки-загрузка, прежде чем подпись начинает печататься. */
export const DEMO_STEP_LOADING_MS = 1000;

/**
 * Сколько ждать после появления подписи, прежде чем начинать точки следующего шага —
 * время, за которое печатающийся эффект (`.demo-step-stream`, 0.6s в globals.css) успевает
 * доиграть на экране. Уточнение владельца 2026-09-03: сначала точки, ЗАТЕМ стрим текста, и
 * только потом снова точки — а не всё сразу.
 */
export const DEMO_STEP_REVEAL_MS = 700;
