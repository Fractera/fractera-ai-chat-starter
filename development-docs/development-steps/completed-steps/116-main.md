# Шаг 116 — ревизия устаревшей документации: одна стратегия вместо двух, два бота вместо многих

**Решение владельца 2026-09-05, дословно:** «мы полностью прекращаем стратегию одновременного
использования библиотеки чата и Telegram, так как подтвердилась гипотеза простого и эффективного
взаимодействия напрямую между Telegram и Claude Code».

Шаг промежуточный и направлен на **глубокий анализ устаревшей документации и её корректировку**, а не
на новую способность. Владелец распорядился идти без остановок и решать самому: «каждый раз, когда
будут у тебя возникать вопросы, не нужно меня спрашивать — действуй из соображений того, что будет
для меня лучше».

## Что теперь единственный путь к ИИ

```
человек в Telegram → плагин каналов Anthropic → живая сессия claude на сервере → обратно в Telegram
```

Нашего кода в цепочке ноль (шаг 115, подтверждено владельцем: «all work»).

## 🔒 Ботов в проекте всегда ДВА — новый закон

| Бот | Чем занят | Состояние |
|---|---|---|
| **№1 — управление агентами и автоматизация** | основная задача проекта, `fractera-agent-channel` | ✅ работает |
| **№2 — управление разработкой** | не начат, не описан | 🛑 **не центр архитектуры** — слово владельца |

🔒 **ТРЕТЬЕГО НЕ ЗАВОДИТЬ, ВТОРОЙ ВПЕРЁД НЕ ПРОЕКТИРОВАТЬ.** Владелец назвал факт существования
второго бота и его место на периферии; всё остальное о нём было бы домыслом. Действует общий закон
«дальнее не проектируется вперёд».

## Что сделано в коде (`fractera-next-starter`)

| Убрано | Как |
|---|---|
| ссылка «Чат с ИИ-агентом» в подвале сайта | островок `chat-link.client.tsx` удалён, словарь `CHAT_LINK` удалён, расчёты `chatUrlFromSite()` и `chatBase()` удалены, вызов в `footer-menu.server.tsx` заменён надгробием |
| раздел «Стратегия автоматизации» в левом меню входа Telegram | `"automation"` вычеркнут из `TELEGRAM_SECTIONS`; удалены `_components/automation-strategy.tsx`, `_components/automation-mode.client.tsx`, `_lib/automation-strategy.ts` и слова обоих словарей |

🔒 **УБРАНО ЦЕЛИКОМ, А НЕ СПРЯТАНО ИЗ МЕНЮ.** `TELEGRAM_SECTIONS` — единственный источник и меню, и
маршрутизации: раздел, вычеркнутый только из меню, остался бы жить по прямому адресу — способность
без входа, о которой знает лишь читавший код. Тот же закон, которым шаг 111 убирал «Команды»,
«Календарь» и «Карту».

🛑 **ДОЛГ НАЗВАН, А НЕ ЗАМОЛЧАН:** ключ `automationMode` остался в `PLATFORM-CONFIG` и в чате
(`lib/fractera/automation-mode.ts`). Двери к нему больше нет; удаление самого ключа задевает схему
конфига и второй репозиторий — это отдельное решение.

**Доказательство:** три гейта зелёные — `check:types` (`===TYPES_OK===`), `check:i18n`
(«все словари полны», 82/82 языков), `check:menu` («манифестов групп: 2, все достижимы обходом
меню»). Негативный контроль дал бы отказ типов на любом осиротевшем импорте: удалены три файла и два
экспорта, все их потребители правились той же операцией.

## Что сделано в документации

| Документ | Правка |
|---|---|
| федеральный `CLAUDE.md` | новый раздел «Девятый и десятый процессы»; закон о двух ботах; запрет `getUpdates`; квота одна на двоих; таблица pm2 доведена до **десяти** процессов ВМЕСТЕ с числом |
| `fractera-next-starter/CLAUDE.md` | 🪦 на утверждение «подвал несёт кнопку чата»; разделов входа Telegram стало **четыре**, число исправлено вместе со списком; 🪦 на пятый раздел |
| `PASSPORT.md` | блок «РЕВИЗИЯ 2026-09-05» перед всеми разделами; 🪦 на § 9.4 и § 10.12; § 9 (конвейер) помечен **приостановленным** |
| chatbot `current-steps.md` | блок ревизии первым; 🪦 на «Стратегию архитектуры чата», «Telegram — не главный вход», «Единственное хранилище»; «вариант A» уточнён числом ботов |
| федеральный `current-steps.md` | блок ревизии первыми десятью строками |
| `CANCELLED.md` | семь записей 2026-09-05 |
| `GLOSSARY.md` | шесть терминов: два бота, канал агента против службы каналов `:3500`, сторож лимита, `Claude` а не `Cloud` |
| репорт фичи 97–105 | шапка: стратегия прекращена, репорт остаётся историей |
| `new-steps/` | планы закрытых 111, 112, 113, 114 и отменённого 108 удалены из очереди (восстанавливаются из git); 106 помечен приостановленным |

## Решения, принятые агентом самостоятельно — названы, а не спрятаны

🔒 **«УБИРАЙ ВСЮ ЛЕВУЮ ВКЛАДКУ» ПРОЧИТАНО КАК «УБИРАЙ ТУ ВКЛАДКУ ЛЕВОГО МЕНЮ».** Владелец назвал
предмет точно: «вкладки в левом меню, которые создавались для цели настройки различных вариантов
работы». Это ровно раздел «Стратегия автоматизации» — выбор между конвейером OpenAI и агентом
Anthropic. Снос всего левого меню унёс бы «Описание», «Логи», «Настройки» и «Паспорт», которые к
вариантам работы отношения не имеют и нужны. Прочтение названо здесь, чтобы владелец мог поправить
одной фразой.

🔒 **РАЗБОР ЗАПРОСА ПРИОСТАНОВЛЕН, А НЕ СНЕСЁН.** Ревизия отменяет стратегию, а не работающий код на
~36 признаков и шесть ступеней. Снос — отдельное решение владельца, и агент не принял его за него.

🔒 **КНОПКА «ЧАТ» В ШАПКЕ ПАНЕЛИ (96-9) НЕ ТРОНУТА.** Владелец сказал «из подвала»; кнопка в панели —
навигация между службами, а не стратегия. Чат `:3600` жив как служба, и попасть в него по-прежнему
есть чем.

## Ошибки этого шага

✗ **Heredoc с кодом на JavaScript порвал внешнюю оболочку.** Попытка записать файл через
`cat > … <<'EOF'` в Bash-инструменте упала на `unexpected EOF`: в теле десятки одинарных кавычек, и
они схлопнулись с внешним `bash -c`. **Лечение — писать такой файл выделенным инструментом записи**,
а не heredoc'ом. Тот же класс, что запрет вложенных кавычек в SSH-обёртке.

✗ **`python` на машине владельца нет вовсе** — есть только заглушка Microsoft Store. Скрипты правки
файлов писать на `node` или делать точечные правки, а `python - <<PY` не использовать.

## Эволюция навыков

Правило, которое стоит перенести в навык `use-development-steps`: **ревизия — отдельный жанр шага**,
и её итог отличается от обычного тем, что главный результат лежит НЕ в коде. Проверка полноты у неё
тоже своя: пройти по всем документам, которые читаются при старте сессии (федеральный `CLAUDE.md`,
гостевой `CLAUDE.md`, оба `current-steps.md`, `PASSPORT.md`), и убедиться, что ни один не описывает
отменённую стратегию как действующую. Документ, оставшийся с прежней правдой, опаснее отсутствующего:
он лежит там, где строят.
