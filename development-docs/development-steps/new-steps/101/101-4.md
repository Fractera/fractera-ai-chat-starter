# 101-4 — фиксация решения в PASSPORT.md (fractera-next-starter)

## 1. Что строится

В `fractera-next-starter/development-docs/PASSPORT.md`:
- §9 «Что решено» — новый пункт, дословные решения владельца о `reasoning` vs `ChainOfThought` и
  о единственном хранилище (`Message_v2.parts`, без отдельной базы).
- §7.0 и §9.4 — снимается формулировка «область размышления не подключена», заменяется на честное
  текущее состояние: каркас подключён (шаг 101), несёт один шаг-заглушку «модель формирует ответ»;
  настоящие шаги разбора запроса — отдельная будущая работа, не спроектированная.
- Подсветка `==…==` (закон §9.5) снимается с прошлого шага и переносится на эту правку.

## 2. Что считается готовым

`PASSPORT.md` не утверждает больше, что область размышления не подключена, рядом стоит правда о
том, что построено сегодня.

## 3. Чем доказывается

- `grep -c "область размышления не подключена" PASSPORT.md` (или её точная старая формулировка) → 0.
- Новый абзац присутствует и обёрнут в `==…==`.

## 4. На что опирается

101-1..101-3 — документ описывает уже построенное, а не план.

## 5. Чего не входит

Код — это шаг только документа, коммит идёт прямо в `main` `fractera-next-starter` (мелкая правка
документации, без цикла ветки шага).
